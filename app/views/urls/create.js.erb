

(function() {

  alert("good kshafd" );
  
  $("#form_url")[0].reset();

  $("a.short-url").text('<%= root_url.to_s() + (@url.id).to_s(36) %>');

  $("a.short-url").attr('href', '<%= url_url(@url.id) %>');

  $("#notice").html('<%=escape_javascript(flash.now[:notice]) %>');


})();
