

(function() {

  alert("good kshafd" );
  
  $("#form_url")[0].reset();

  $("a.short-url").text('<%= url_url((@url.id).to_s(36))%>');

  $("a.short-url").attr('href', '<%= url_url((@url.id).to_s(36)) %>');

  $("#notice").html('<%=escape_javascript(flash.now[:notice]) %>');


})();
